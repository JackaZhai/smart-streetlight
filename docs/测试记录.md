# 测试记录

## 计划验证项

| 项目 | 命令 | 目标 |
| --- | --- | --- |
| 后端领域测试 | `npm --workspace backend test` | 验证遥测入库、自动控制、离线告警和阈值统计 |
| MySQL 集成测试 | `TEST_DATABASE_URL=mysql://streetlight:streetlight_pass@127.0.0.1:3307/smart_streetlight npm --workspace backend test -- tests/mysqlStore.test.ts` | 验证 MySQL 建表、seed、更新和持久化 |
| 全量测试 | `npm test` | 运行所有 workspace 测试 |
| 类型检查 | `npm run typecheck` | 验证 TypeScript 类型 |
| 构建 | `npm run build` | 验证前端、后端、mock-device 可编译 |
| Compose 静态校验 | `docker compose -f deploy/docker-compose.yml config` | 验证全栈 Compose 配置可解析 |
| 认证与校验测试 | `npm --workspace backend test -- tests/auth.test.ts tests/validation.test.ts` | 验证登录 token 和 API 参数校验规则 |

## 人工联调流程

1. `cp .env.example .env`
2. `docker compose -f deploy/docker-compose.yml up -d`
3. `npm run dev:backend`
4. `npm run dev:frontend`
5. `npm run dev:mock`
6. 打开 `http://localhost:5173`
7. 检查实时光照、历史趋势、设备表格、告警列表和智能问答。
8. 点击“开灯/关灯”，确认 mock-device 输出执行日志。

## MySQL 验收点

1. `docker compose -f deploy/docker-compose.yml ps` 显示 `smart-streetlight-frontend`、`smart-streetlight-backend`、`smart-streetlight-mysql` 和 `smart-streetlight-emqx` 均运行。
2. `curl -sS http://127.0.0.1:4000/health` 返回 `storageDriver=mysql`。
3. `POST /api/auth/login` 使用 `admin/admin123` 能返回 token。
4. 未携带 token 访问 `/api/overview` 返回 `401`。
5. 携带 token 启动 `mock-device` 后，`/api/overview` 能看到最新光照和在线设备。
6. 重启后端后，设备、阈值、告警和光照历史仍可从 MySQL 读取。

## 全栈 Docker Compose 验收点

1. `docker compose -f deploy/docker-compose.yml up -d --build` 能构建并启动四个服务。
2. `curl -sS -I http://127.0.0.1:18080` 返回 `200 OK`。
3. `curl -sS http://127.0.0.1:18080/health` 通过前端 Nginx 反代到后端，并返回 `storageDriver=mysql`。
4. 浏览器访问 `http://127.0.0.1:18080` 能打开前端页面。
5. 登录页使用 `admin/admin123` 可进入控制台。

## Spark 实测记录

2026-07-02 在 `spark-2390` 的 `/home/cavin-dgx/Desktop/smart-streetlight` 验证：

| 项目 | 结果 |
| --- | --- |
| Docker Compose | `smart-streetlight-emqx`、`smart-streetlight-mysql` 均已启动 |
| MySQL 集成测试 | `TEST_DATABASE_URL=mysql://streetlight:streetlight_pass@127.0.0.1:3307/smart_streetlight npm --workspace backend test -- tests/mysqlStore.test.ts` 通过 |
| 全量测试 | `npm test` 通过，普通运行下 MySQL 集成测试按设计跳过 |
| 类型检查 | `npm run typecheck` 通过 |
| 构建 | `npm run build` 通过 |
| 后端健康检查 | `http://127.0.0.1:4100/health` 返回 `storageDriver=mysql` |
| MySQL 持久化 | 新增测试设备后重启后端，设备仍可从 `/api/devices/{id}` 读取 |
| MQTT 联调 | `mock-device` 成功连接 EMQX，`/api/overview` 返回最新读数 |

## Spark 全栈 Compose 实测记录

2026-07-02 在 `spark-2390` 的 `/home/cavin-dgx/Desktop/smart-streetlight` 验证：

| 项目 | 结果 |
| --- | --- |
| 镜像构建 | `docker compose -f deploy/docker-compose.yml build backend frontend` 通过 |
| 全栈启动 | `docker compose -f deploy/docker-compose.yml up -d` 启动 `frontend`、`backend`、`emqx`、`mysql` 四个服务 |
| 前端端口 | 由于 Spark 已有服务占用 `8080` 和 `8088`，本项目默认使用 `18080:80` |
| 前端入口 | `curl -sS -I http://127.0.0.1:18080` 返回 `200 OK` |
| Nginx 反代健康检查 | `curl -sS http://127.0.0.1:18080/health` 返回 `storageDriver=mysql` |
| 后端直连健康检查 | `curl -sS http://127.0.0.1:4000/health` 返回 `storageDriver=mysql` |
| API 反代 | `curl -sS http://127.0.0.1:18080/api/overview` 返回设备、最新读数和告警统计 |
| Socket.IO 反代 | `/socket.io/?EIO=4&transport=polling` 返回 HTTP `200` |
| MQTT 联调 | `mock-device` 成功连接 `mqtt://127.0.0.1:1883`，容器化后端继续返回最新读数 |
