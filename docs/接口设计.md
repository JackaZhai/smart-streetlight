# 接口设计

## REST API

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/health` | 后端健康检查 |
| POST | `/api/auth/login` | 登录并获取访问令牌 |
| GET | `/api/auth/me` | 查询当前登录用户 |
| GET | `/api/overview` | 获取总览、设备、最新光照、告警和阈值 |
| GET | `/api/devices` | 获取设备列表 |
| GET | `/api/audit-logs` | 查询最近审计日志，要求 `admin` |
| GET | `/api/fault-predictions` | 查询全部设备故障预判 |
| GET | `/api/devices/{id}` | 获取设备详情 |
| POST | `/api/devices` | 新增设备，要求 `admin` |
| GET | `/api/devices/{id}/latest-light` | 获取最新光照 |
| GET | `/api/devices/{id}/light-history` | 获取历史光照 |
| GET | `/api/devices/{id}/fault-predictions` | 查询单设备故障预判 |
| GET | `/api/devices/{id}/threshold` | 获取阈值 |
| PUT | `/api/devices/{id}/threshold` | 更新阈值，要求 `admin` 或 `operator` |
| POST | `/api/devices/{id}/commands` | 下发开灯/关灯指令，要求 `admin` 或 `operator` |
| GET | `/api/alarms` | 查询告警 |
| PUT | `/api/alarms/{id}/handle` | 提交处理备注并标记告警已处理，要求 `admin` 或 `operator` |
| POST | `/api/agent/chat` | 智能问答 |

除 `/health` 和 `/api/auth/login` 外，所有 `/api` 接口都需要请求头：

```http
Authorization: Bearer <token>
```

角色说明：

| 角色 | 权限 |
| --- | --- |
| `admin` | 所有读写接口、设备新增、审计日志查询 |
| `operator` | 普通查询、阈值调整、开关灯控制、告警处理 |
| `viewer` | 普通查询和智能问答，只读 |

## 健康检查响应

```json
{
  "ok": true,
  "service": "smart-streetlight-backend",
  "storageDriver": "mysql",
  "timestamp": "2026-07-02T01:00:00.000Z"
}
```

## 登录请求

```json
{
  "username": "admin",
  "password": "admin123"
}
```

登录成功响应：

```json
{
  "token": "<signed-token>",
  "user": {
    "username": "admin",
    "role": "admin"
  },
  "expiresAt": "2026-07-03T01:00:00.000Z"
}
```

## 审计日志响应

```json
[
  {
    "id": "audit-xxxx",
    "actorUsername": "operator",
    "actorRole": "operator",
    "action": "command.dispatch",
    "targetType": "device",
    "targetId": "SL-001",
    "result": "SUCCESS",
    "detail": "TURN_ON",
    "createdAt": "2026-07-02T01:00:00.000Z"
  }
]
```

审计日志记录成功的设备新增、阈值调整、手动控制、告警处理，以及被角色权限拒绝的写操作。告警处理的 `detail` 会保存处理备注，便于管理员复盘。

## 控制指令请求

```json
{
  "command": "TURN_ON"
}
```

`command` 可取值：

- `TURN_ON`
- `TURN_OFF`

## 告警处理请求

```json
{
  "remark": "已通知现场人员检查传感器遮挡，设备恢复后复核。"
}
```

`remark` 为处理备注，长度 1-300 个字符。处理成功后，告警对象会包含处理元数据：

```json
{
  "id": "alarm-xxxx",
  "deviceId": "SL-002",
  "alarmType": "SENSOR_ANOMALY",
  "alarmLevel": "WARN",
  "alarmContent": "光照数据连续波动异常，建议检查传感器遮挡情况。",
  "handled": true,
  "handledBy": "operator",
  "handledAt": "2026-07-02T08:30:00.000Z",
  "handleRemark": "已通知现场人员检查传感器遮挡，设备恢复后复核。",
  "createdAt": "2026-07-02T08:00:00.000Z"
}
```

## 故障预判响应

```json
[
  {
    "id": "fault-xxxx",
    "deviceId": "SL-001",
    "riskType": "CONTROL_FAILURE",
    "riskLevel": "MEDIUM",
    "reason": "最近存在控灯指令失败或回执超时，继电器、通信或设备执行链路可能异常。",
    "suggestedAction": "复核 MQTT command/reply、设备继电器状态和现场灯具执行结果。",
    "evidence": ["2026-07-03T08:04:00.000Z TURN_ON manual FAILED"],
    "createdAt": "2026-07-03T08:05:00.000Z"
  }
]
```

`riskType` 可取值：

- `HEARTBEAT_TIMEOUT`
- `LIGHT_SENSOR_ANOMALY`
- `CONTROL_FAILURE`
- `ALARM_BACKLOG`

`riskLevel` 可取值：

- `LOW`
- `MEDIUM`
- `HIGH`

## 智能问答请求

```json
{
  "question": "设备离线后应该怎么排查？"
}
```

智能问答响应：

```json
{
  "answer": "针对 SL-003，设备离线通常与供电、网络、MQTT 连接或设备编号配置有关...",
  "references": ["设备离线排查", "MQTT 主题说明"],
  "matches": [
    {
      "id": "kb-offline",
      "title": "设备离线排查",
      "source": "智慧路灯运维知识库 / 设备离线排查",
      "snippet": "设备离线通常与供电、网络、MQTT 连接或设备编号配置有关...",
      "score": 12,
      "keywords": ["离线", "掉线", "心跳"]
    }
  ],
  "suggestedActions": ["检查设备供电和网络连接", "确认 MQTT 客户端在线"]
}
```

`matches` 和 `suggestedActions` 为本地 RAG 知识库增强字段，旧前端仍可只使用 `answer` 和 `references`。

## Socket.IO 事件

| 事件 | 方向 | 说明 |
| --- | --- | --- |
| `state:update` | 后端到前端 | 推送总览数据、设备状态、告警和阈值 |

Socket.IO 连接需要在 `auth.token` 中携带登录 token。
